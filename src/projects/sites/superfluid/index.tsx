'use client';

export default function SuperfluidSite() {
    return (
        <iframe
            src="https://superfluid.work"
            className="w-full h-full border-none"
            title="Superfluid"
        />
    );
}
